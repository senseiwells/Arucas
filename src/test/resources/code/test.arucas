// ./src/test/resources/code/test.arucas


start = getNanoTime();
local piSum: Number = 0;
for (n = 1; n < 1000000; n++) {
    piSum += 1/(n*n);
}

local pi = Math.sqrt(piSum * 6);

print(getNanoTime() - start);
print(pi);
