// run(File.getDirectory().getPath() + "/src/test/resources/code/test.arucas");

discordBot = new DiscordBot("");
discordBot.setActivity("playing", "Arucas!");

disCommand = {
    "name": "testcommand",
    "description": "This is a test command",
    "command": fun(event) {
        print("no parameters!");
    },

    "next": {
        "name": "sub",
        "option": "String",
        "description": "extra secret",
        "command": fun(event, p1) {
            event.reply(p1);
        }
    }
};

discordBot.addCommand(disCommand);

while (true) {
    sleep(100000);
}