// ./src/test/resources/code/test.arucas

routine MinecrafTask {
    something();
    delay(1000);
    something();
}

new MinecraftTask()
    .then(something)
    .delay(1000)
    .then(something);

routine MinecraftTask {
    something();

    for (i = 0; i < 10; i++) {
        something();
        delay(100);
    }
}

task = new MinecrafTask()
    .then(something);
    .then(fun() {
        for (i = 0; i < 10; i++) {
            something();
            delay(100);
        }
    });