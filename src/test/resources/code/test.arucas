// ./src/test/resources/code/test.arucas
// run(File.getDirectory().getPath() + "/src/test/resources/code/test.arucas");

class B {
    operator + (str: String): String {
        return str;
    }
}

fun add(param: B, otherParam: String): String {
    return param + otherParam;
}

print(add(new B(), "98"));