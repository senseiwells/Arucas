// ./src/test/resources/code/test.arucas
// run(File.getDirectory().getPath() + "/src/test/resources/code/test.arucas");

class B {
    as Number {
        return 10;
    }

    as Boolean {
        return false;
    }
}

print(new B().isNaN());

// Auto casting only works when
// calling members on that value
// or when passing it into a function
// that has specific type requirements
if (new B()) {
    // throws
}
