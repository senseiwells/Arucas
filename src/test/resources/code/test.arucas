// run(getDirectory() + "/src/test/resources/code/test.arucas");

str = "random";
obj = 10;

switch (10) {
    case null -> {
        print("we are u");
    }
    case obj -> {
        print("this will never get called");
    }
    case 10 -> {
        print("ten");
    }
    case str, true -> {
        print("mgm");
    }
}
