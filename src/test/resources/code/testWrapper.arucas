// run(getDirectory() + "/src/test/resources/code/testWrapper.arucas");

test = new Test();

start = millis();
i = 0;
while(i < 1000000) {
    i++;
    test.benchmark([], {}, 'string');
}

print('%s ms'.formatted([millis() - start]));