// run(File.getDirectory().getPath() + "/src/test/resources/code/test2.arucas");

class Example {
    embed String as s;

    Example() {
        this.s = "hi/there";
    }
}

f = Function.getMethod(new Example(), "split", 1);

print(Type.of(f));
print(Type.of(new Example()));

print(f("/"));
