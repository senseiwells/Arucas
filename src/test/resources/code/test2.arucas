// run(File.getDirectory().getPath() + "/src/test/resources/code/test2.arucas");

import Java from util.Internal;

debug(true);

testClassName = "me.senseiwells.impl.Test";
testInstance = Java.constructClass(testClassName);
del = Java.getStaticMethodDelegate(testClassName, "F", 0);

r = Java.callStaticMethod(testClassName, "G");

print(r.toArucasValue());

