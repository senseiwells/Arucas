// run(getDirectory() + "/src/test/resources/code/test2.arucas");

listUtils = run(getDirectory() + "/src/test/resources/code/ListUtils.arucas");
numberUtils = run(getDirectory() + "/src/test/resources/code/NumberUtils.arucas");
stringUtils = run(getDirectory() + "/src/test/resources/code/StringUtils.arucas");

shuffle = listUtils.get("shuffle");
randomI = listUtils.get("random");
remap = listUtils.get("remap");
filter = listUtils.get("filter");
trim = stringUtils.get("trim");

list = [2, 3, 1, 3, 4];

list = filter(remap(list, fun(value) {
    if (value == 1) {
        return "one";
    }
    if (value == 2) {
        return "two";
    }
}), fun(value) {
    return value != null;
});

print(list);

print(numberUtils.get("e"));
print(trim("  hi  my      name is  Sensei"));