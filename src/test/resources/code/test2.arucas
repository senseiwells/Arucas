// run(getDirectory() + "/src/test/resources/code/test2.arucas");

stringUtils = run(getDirectory() + "/src/test/resources/code/ListUtils.arucas");

filterList = stringUtils.get("filterList");
indexOf = stringUtils.get("indexOf");

list = [2, 3, 1, 3, 4];

indexes = indexOf(list, 1);

print(indexes);